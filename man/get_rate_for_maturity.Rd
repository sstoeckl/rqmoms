% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmoms_utils.R
\name{get_rate_for_maturity}
\alias{get_rate_for_maturity}
\title{Interpolate a zero rate for a given maturity (or merge rates into a surface)}
\usage{
get_rate_for_maturity(df_rate, df_surf = NULL, date = NULL, days = NULL)
}
\arguments{
\item{df_rate}{Data frame with \code{date}, \code{days}, \code{rate} (decimal p.a.).}

\item{df_surf}{Optional surface with \code{date}, \code{days}. If supplied, returns
\code{df_surf} with a \code{rate} column merged by date/maturity.}

\item{date}{Optional single \code{Date} for scalar interpolation.}

\item{days}{Optional single integer days-to-maturity for scalar interpolation.}
}
\value{
A numeric rate (if \code{date} & \code{days} are given) or a data frame with
a \code{rate} column (if \code{df_surf} is given).
}
\description{
Interpolates the zero curve by \code{date} and \code{days}, returning either a single
scalar rate (when \code{date} and \code{days} are given) or merging a \code{rate} column
into a surface data frame (\code{df_surf}).
}
\examples{
# scalar interpolation
one <- subset(qmoms_surface, id == qmoms_surface$id[1] & days == qmoms_surface$days[1])
get_rate_for_maturity(qmoms_zerocd, date = one$date[1], days = one$days[1])

# merge into a surface
head(get_rate_for_maturity(qmoms_zerocd, df_surf = qmoms_surface))

}
