% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmoms_utils.R
\name{filter_options}
\alias{filter_options}
\title{Filter option rows by moneyness, delta windows and quality flags}
\usage{
filter_options(optdata, filter)
}
\arguments{
\item{optdata}{A data frame with columns \code{mnes}, optional \code{delta},
and optionally \code{open_interest}, \code{best_bid}, \code{best_offer}, \code{prem}.}

\item{filter}{A list with bounds (see \code{\link[=rq_default_params]{rq_default_params()}}$filter),
e.g. \code{mnes_lim}, \code{delta_put_lim}, \code{delta_call_lim}, \code{open_int_zero},
\code{best_bid_zero}, \code{min_price}.}
}
\value{
A filtered data frame (sorted by \code{id}, \code{date}, \code{mnes} if present).
}
\description{
Mirrors the Python helper: removes in-the-money quotes and rows
outside delta windows; optionally enforces open interest / bid / min price.
}
\examples{
# keep only OTM & within delta windows
head(filter_options(qmoms_surface, rq_default_params()$filter))
}
